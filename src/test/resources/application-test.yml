spring:
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
  cache:
    type: none
  data:
    redis:
      host: localhost
      port: 6379

user:
  service:
    url: http://userservice:8083